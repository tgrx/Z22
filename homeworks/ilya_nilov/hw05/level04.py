def host(hstnam):
    from urllib.parse import urlparse
    if not hstnam:
        return ""
    listurl = urlparse(hstnam).netloc + urlparse(hstnam).path  
    listurl.
    return listurl[:]
#еще не готово
    
